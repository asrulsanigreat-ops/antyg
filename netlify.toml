[build]
  command = "pip install -r requirements.txt"
  publish = "app/static"

[functions]
  directory = "netlify/functions"
  included_files = ["app/**"]

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
